# rePROMPT — Environment Configuration
# Copy this file to .env and set your Anthropic API key.
#
#   cp .env.example .env
#
# NEVER commit the .env file — it is excluded via .gitignore.

# ── AI Provider Configuration ────────────
# Option A: Direct Anthropic API (set your API key)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Option B: AI Gateway (Vercel AI Gateway, custom proxy, etc.)
# When set, requests are routed through this gateway instead of
# directly to api.anthropic.com.  The URL should point to the
# gateway's base path (e.g. https://gateway.ai.vercel.sh/v1).
# AI_GATEWAY_URL=

# Optional: separate API key for the gateway (falls back to ANTHROPIC_API_KEY).
# AI_GATEWAY_API_KEY=

# ── Server Configuration ─────────────────
# Optional port for local backend server.
PORT=3000

# Optional frontend override when serving static assets from a separate host.
# Example: https://api.example.com
REPROMPT_API_BASE_URL=

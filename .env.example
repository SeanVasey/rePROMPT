# rePROMPT — Environment Configuration
# Copy this file to .env and set credentials for your deployment target.
#
#   cp .env.example .env
#
# NEVER commit the .env file — it is excluded via .gitignore.

# ── AI Provider Configuration ────────────
# Option A: Direct Anthropic API (recommended baseline)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Option B: AI Gateway (Vercel AI Gateway, custom proxy, etc.)
# Set the gateway URL; supports either a base URL or full /messages endpoint.
# Example base URL: https://gateway.ai.vercel.sh/v1
AI_GATEWAY_URL=

# Optional path when AI_GATEWAY_URL points to a base URL.
# Default: /messages
AI_GATEWAY_MESSAGES_PATH=/messages

# Optional gateway credential (falls back to ANTHROPIC_API_KEY when empty).
AI_GATEWAY_API_KEY=

# Authentication mode for gateway requests:
# - x-api-key (default for Anthropic-compatible gateways)
# - bearer (common for Vercel AI Gateway access tokens)
AI_GATEWAY_AUTH_MODE=x-api-key

# ── Server Configuration ─────────────────
PORT=3000

# Optional frontend override when serving static assets from a separate host.
# Example: https://api.example.com
REPROMPT_API_BASE_URL=
